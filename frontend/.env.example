# Example .env file for the frontend
#
# Create a .env file in this directory (frontend/.env) and
# add the following variable if your backend is not running on the default URL.

# URL of the backend API
VITE_API_BASE_URL=http://127.0.0.1:8000/
